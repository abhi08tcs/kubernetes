apiVersion: v1
kind: Service
metadata: 
  labels:
    name: service-file
    purpose: to connect to multiple nodes
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - targetPort: 80
      port: 80
       
